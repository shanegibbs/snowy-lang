language: cpp
compiler:
  - gcc
  - clang
os:
  - linux
before_install:
install:
before_script:
  - mkdir m4 && ./autogen.sh
script:
  - ./configure CXXFLAGS='-std=c++11' && make && make check
after_failure:
  - cat config.log
sudo: false
