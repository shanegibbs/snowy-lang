
* create configure.ac
* # touch NEWS README AUTHORS ChangeLog
* # autoreconf -iv
* # mkdir build; cd build
* # ../configure CC=clang LDFLAGS=-L/usr/local/lib CFLAGS=-I/usr/local/include --enable-debug

Notes:

List symbols in a library:
# nm -gU <lib>
